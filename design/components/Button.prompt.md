Button. Primary = volt (the user's action — one per view, uppercase copy).
```jsx
<Button>COMPLETE MATCHWEEK →</Button>
<Button variant="secondary">View rules</Button>
```
Variants: primary/secondary/ghost/danger. 44px min height.