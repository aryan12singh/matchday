Fixture-vote toggle + live tally bar (screen 21). Votes are advisory; identities never shown.
```jsx
<VoteTallyBar votes={5} max={7} mine onToggle={toggle} />
<VoteTallyBar votes={2} max={7} disabled />
```