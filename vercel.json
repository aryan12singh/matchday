{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @matchday/web build",
  "outputDirectory": "apps/web/.next",
  "github": { "silent": true }
}
